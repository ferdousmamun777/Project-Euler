def s(n):
    sum=0
    while n!=0:
        a=n%10
        sum=sum+a
        b=n//10
        n=b
    return sum

def fact(n):
    sum=1
    for i in range(1, n+1):
        sum=sum*i

    a=s(sum)
    return a

print(fact(100))
